apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: documind
  labels:
    app.kubernetes.io/name: ai-service
    app.kubernetes.io/part-of: documind
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
