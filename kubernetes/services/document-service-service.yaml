apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: documind
  labels:
    app.kubernetes.io/name: document-service
    app.kubernetes.io/part-of: documind
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: document-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
