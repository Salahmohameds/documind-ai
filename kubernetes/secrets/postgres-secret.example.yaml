# TEMPLATE ONLY — never commit a filled-in copy of this file.
# TEMPORARY resource, see postgres-init-scripts-configmap.yaml header.
#
#   kubectl -n documind create secret generic postgres-secret \
#     --from-literal=POSTGRES_PASSWORD="$(openssl rand -base64 24)"
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: documind
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: documind
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
