# TEMPLATE ONLY — never commit a filled-in copy of this file.
# CONTRIBUTING.md rule 1: only *.example files, real values via Vault/kubectl.
#
#   kubectl -n documind create secret generic search-service-secret \
#     --from-literal=DB_PASSWORD='REPLACE_ME' \
#     --from-literal=JWT_SECRET='REPLACE_ME' \
#     --from-literal=AI_SERVICE_AUTH_TOKEN='REPLACE_ME'
#
# JWT_SECRET MUST equal api-gateway's JWT_SECRET (kubernetes/secrets/api-gateway-secret.example.yaml).
apiVersion: v1
kind: Secret
metadata:
  name: search-service-secret
  namespace: documind
  labels:
    app.kubernetes.io/name: search-service
    app.kubernetes.io/part-of: documind
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
  AI_SERVICE_AUTH_TOKEN: ""
