# TEMPLATE ONLY — never commit a filled-in copy of this file.
# CONTRIBUTING.md rule 1: only *.example files, real values via Vault/kubectl.
#
# Only needed if AI_BACKEND=openai_compat (not the demo default — see the
# ConfigMap). Leave empty/omit the Secret entirely when AI_BACKEND=mock|oci.
#
#   kubectl -n documind create secret generic ai-service-secret \
#     --from-literal=OPENAI_API_KEY='REPLACE_ME'
apiVersion: v1
kind: Secret
metadata:
  name: ai-service-secret
  namespace: documind
  labels:
    app.kubernetes.io/name: ai-service
    app.kubernetes.io/part-of: documind
type: Opaque
stringData:
  OPENAI_API_KEY: ""
