[pytest]
# Modern import mode: lets identically-named test modules coexist in
# different directories without __init__.py files.
addopts = -m "not disruptive and not accuracy" --import-mode=importlib
markers =
    disruptive: stops and starts real containers; mutates system state
    accuracy: uploads the corpus and measures result quality; slow
testpaths = tests