# Optional extra — ONLY needed when STORAGE_TYPE=oci_object_storage.
#
# Deliberately kept out of requirements.txt so the default image stays small
# and the service can build, run and test with no OCI dependency at all.
# The Dockerfile installs this layer only when INSTALL_OCI=true. Version
# pinned to match services/processing-service/requirements-oci.txt and
# services/ai-service/requirements-oci.txt.
oci==2.149.1
