# DocuMind AI — Root State Backend Configuration
# Default: Local state (allows instant terraform init & terraform plan)
# For OCI S3 Remote State: Uncomment backend "s3" block below & run:
#   terraform init -backend-config=backend.hcl

# terraform {
#   backend "s3" {}
# }
